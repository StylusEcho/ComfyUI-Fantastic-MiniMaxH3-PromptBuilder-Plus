[project]
# Registry IDs are lowercase-and-hyphens only, so this is the registry-legal
# spelling of Fantastic-MiniMax-H3-PromptStudio. DisplayName below carries the
# cased form users actually see.
name = "fantastic-minimax-h3-promptstudio"
# 2.0.0 broke saved workflows twice over, deliberately, in one release rather
# than spending two: it dropped the standalone Prompt Builder / Media Loader /
# Reference Splitter / Filename Prefix nodes in favour of Prompt Studio alone,
# and reordered Prompt Studio's outputs to put model first. Slots are positional,
# so links out of a pre-2.0.0 Prompt Studio come back one slot off and need
# reconnecting. See "Upgrading from 1.x" in the README.
# 2.1.0 folds in upstream 1.5.5/1.5.6 (node/text size controls replacing the
# old size-preset button, a crop-render crash fix, and the picture "bake"
# route) — no further breakage.
# 2.2.0 folds in upstream 1.5.8 (atomic writes for presets/prompts, a save
# collision check and an explicit rename flag for the prompt library, and
# dimension/duration healing for presets saved before that metadata was
# stored) — again no breakage of saved workflows.
# 2.3.0 folds in upstream 1.6.0 (Draft mode, the editor's own Media button
# opening the loader over the top of it, a pack-owned preset picker, and the
# [Shot N] chip's caret-drift fix) — no breakage of saved workflows.
# 2.4.0 renames the picture_1/picture_2 outputs to first_frame/last_frame,
# matching what MiniMaxH3ImageToVideo actually calls them. For L2VA only this
# also moves which slot the picture comes out on — its one loaded picture
# used to travel on picture_1, and now travels on last_frame, since that is
# what it actually is. I2VA and FL2VA output on the same two slots as before.
# See "Upgrading from 1.x" in the README.
# 2.5.0 folds in upstream 1.6.1 (prompt-to-preset linking, preset categories,
# the HTML guide replacing the PDF, save-on-close) and the 1.6.2 security
# release (path containment, per-route CSRF guards, PyAV-only decoding with
# no subprocess use) — no breakage of saved workflows.
# 2.6.0 folds in upstream 1.6.3 / 1.6.4 / 1.7.0 / 1.7.1: the registry
# policy-v0.2 hardening (session-token guard on every state-changing route),
# a crop-after-rotate fix, the delivery-tag picker, and the whole RefMod
# subsystem — which adds nodes to a pack that had settled on one. See
# "Upgrading from 1.x" in the README.
# 2.7.0 folds in upstream 1.7.2 / 1.7.3: subject names (a name box per
# <Subject N> line, !Name shorthand with Tab completion, and RefMods that
# carry a subject name, appearance and voice description), voiced speaker
# buttons, the right-click tag menu, and the rebuilt twelve-slot RefMod
# Stack node with presets — no breakage of saved workflows.
# 2.8.0 folds the RefMod Stack into Prompt Studio as a RefMods tab (a hidden
# stack_state widget, appended last so older workflows load it empty); the
# standalone Stack node stays for chaining — no breakage of saved workflows.
version = "2.8.0"
description = "MiniMax H3 Prompt Studio: a single guided prompt-writing and reference-media node for the H3 video model, with live checking against MiniMax's prompt guide."
license = { file = "LICENSE" }
requires-python = ">=3.9"
classifiers = [
    "Operating System :: OS Independent",
]

# PyAV is only needed for reference videos; images and audio work without it,
# and ffmpeg on PATH is an equally good alternative. It is declared here so a
# Manager install works for video out of the box. Left unpinned to avoid
# clashing with other packs that pin their own version.
dependencies = [
    "av",
]

[project.urls]
Repository = "https://github.com/StylusEcho/ComfyUI-Fantastic-MiniMaxH3-PromptBuilder-Plus"
"Bug Tracker" = "https://github.com/StylusEcho/ComfyUI-Fantastic-MiniMaxH3-PromptBuilder-Plus/issues"

[tool.comfy]
PublisherId = "adudeguyman"
DisplayName = "Fantastic MiniMax H3 Prompt Studio"
requires-comfyui = ">=0.30.0"
